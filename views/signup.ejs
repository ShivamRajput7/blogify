<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sign-Up</title>

    <%- include('./partials/head') %>
  </head>
  <body>
    <div class="container mt-5">
      <div class="row justify-content-center">
        <div class="col-md-6">
          <div class="card p-4 shadow-sm">
            <h3 class="text-center mb-4 fourth-color">Sign Up</h3>
            <form method="post">
              <div class="mb-3">
                <label for="name" class="form-label">Name</label>
                <input
                  type="text"
                  class="form-control"
                  id="name"
                  placeholder="Enter your name"
                  required
                  name="name"
                />
              </div>
              <div class="mb-3">
                <label for="email" class="form-label">Email</label>
                <input
                  type="email"
                  class="form-control"
                  id="email"
                  placeholder="Enter your email"
                  required
                  name="email"
                />
              </div>
              <div class="mb-3">
                <label for="password" class="form-label">Password</label>
                <input
                  type="password"
                  class="form-control"
                  id="password"
                  placeholder="Enter your password"
                  required
                  name="password"
                />
              </div>
              <button
                type="submit"
                class="btn second-color text-light w-100 mt-3"
              >
                Sign Up
              </button>
            </form>
          </div>
          <p class="text-center mt-3">
            If you have any account allready,
            <a href="/login" class="text-decoration-none second-color"
              >Login here</a
            >.
          </p>
          <% if (locals.messege) { %>
          <div class="alert alert-danger mt-3" role="alert">
            <span><b><%- messege %></b></span>
            This email allready exits in our database
          </div>
          <% } %>
        </div>
      </div>
    </div>

    <%- include('./partials/script') %>
  </body>
</html>
